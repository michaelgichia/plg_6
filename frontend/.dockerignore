# Ignore node_modules on host (will be installed in container)
node_modules
npm-debug.log
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment files (pass as secrets or env vars instead)
.env
.env*.local

# OS & editor files
.DS_Store
*.swp
*.swo
.vscode
.idea
*.log

# Git & other VCS files
.git
.gitignore
.gitattributes

# Build output (will be regenerated in container)
.next
out

# Tests, coverage, docs (not needed in production image)
__tests__
coverage
*.md

# Dockerfile itself is fine to include, but you can exclude docker-compose overrides
docker-compose*.yml